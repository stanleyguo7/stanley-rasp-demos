# guess-number

一个适配手机端的 4 位数字猜谜小游戏（纯前端，无需后端）。

## 玩法

- 系统随机生成一个 4 位数字（允许重复数字）
- 每轮输入一个 4 位数字进行猜测
- 系统返回：
  - **位置+数字都对** 的个数
  - **数字对但位置错** 的个数
- 当“位置+数字都对 = 4”时通关，并显示总轮次

## 运行

直接用浏览器打开 `index.html` 即可。

## 目录

- `index.html`：页面结构
- `styles.css`：样式（含移动端适配）
- `script.js`：游戏逻辑
